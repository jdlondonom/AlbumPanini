# Copia este archivo como .env.local mediante `npm run setup`.
# Genera una clave distinta para cada instalación; nunca la subas a GitHub.
AUTH_ENCRYPTION_KEY=BASE64_DE_32_BYTES
AUTH_DATABASE_PATH=./data/auth.sqlite
HOST=127.0.0.1
PORT=3010
NODE_ENV=development
