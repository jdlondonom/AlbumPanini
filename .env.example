# Sólo para el entorno local de Rancher Desktop. Nunca uses estos valores en Vercel.
POSTGRES_USER=panini
POSTGRES_PASSWORD=panini_local_only_change_me
POSTGRES_DB=panini_dev

# Debe decodificar a 32 bytes. Genera una diferente antes de compartir el entorno.
# node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
AUTH_ENCRYPTION_KEY=REEMPLAZA_POR_UNA_CLAVE_BASE64_DE_32_BYTES
